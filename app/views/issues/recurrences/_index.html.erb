<% if @recurrences.present? %>
  <table class="list odd-even">
    <% @recurrences.each do |r| %>
      <% if r.visible? %>
        <tr id="recurrence-<%= r.id %>" class="<%= cycle('odd', 'even') %>">
          <td class="description"><%= r.to_s %></td>
          <td class="description">
            <p><nobr><%= last_recurrence(r) %></nobr></p>
            <p><nobr><%= recurrence_nexts(@next_dates[r]) %></nobr></p>
            <p><nobr><%= recurrence_predicts(@predicted_dates[r]) %></nobr></p>
          </td>
          <td class="buttons"><%= delete_button(r) %></td>
        </tr>
      <% end %>
    <% end %>
    <% reset_cycle %>
  </table>
<% end %>
